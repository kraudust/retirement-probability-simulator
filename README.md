# Install Dependencies in Virtual Environment
```bash
rm -rf .venv
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

# Running
Update the yaml file iwth your parameters and then run with the following.
```bash
python3 retirement_age_calculator.py
```
